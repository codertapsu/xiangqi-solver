# Dependencies
node_modules

# Build output
dist

# Release bundle (scripts/build-release.sh) — contains .env + the engine binary.
release/

# Environment
.env

# Test coverage
coverage

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
.idea
*.tsbuildinfo

# Hint wallet (SQLite) — runtime data, never commit.
/data/
*.db
*.db-wal
*.db-shm
